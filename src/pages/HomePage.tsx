import { StudentDashboard } from '../components/student-dashboard';

const HomePage = () => {
    return (
        <div>
            <StudentDashboard />
        </div>
    );
};

export default HomePage;
